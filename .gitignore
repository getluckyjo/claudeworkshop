node_modules/
.DS_Store
*.log
.env
.env.local

# dist/launchpad.zip IS committed on purpose — it's what the cohort downloads.

# Handout HTML is an intermediate — the PDFs are the deliverable
handouts/*.html

# Python bytecode
__pycache__/
*.pyc

# Invoice HTML is an intermediate — the PDFs are the deliverable
invoices/*.html

# The assembled pack is generated — rebuild it rather than committing it
Entrepreneur Coach — *.zip

# Note HTML is an intermediate — the PDFs are the deliverable
notes/*.html
notes/note.css
